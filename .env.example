# Copy to .env and fill in. .env is gitignored -- never commit a key.

# --- test phase: Zhipu GLM via its Anthropic-compatible endpoint ---
KSA_PROVIDER=glm
GLM_API_KEY=
# International endpoint instead of open.bigmodel.cn:
# KSA_BASE_URL=https://api.z.ai/api/anthropic

# --- Anthropic direct ---
# KSA_PROVIDER=anthropic
# ANTHROPIC_API_KEY=

# --- Bedrock, once the AWS account is allowlisted ---
# KSA_PROVIDER=bedrock
# AWS_PROFILE_KSA=personal-aws
# AWS_REGION_KSA=us-west-2

# --- local, no API key ---
# KSA_PROVIDER=ollama

# Optional per-role model overrides, any provider:
# KSA_MODEL_FAST=
# KSA_MODEL_REASONING=
